spring:
  application:
    name: examinai
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
  jpa:
    open-in-view: false
  thymeleaf:
    check-template-location: false

management:
  endpoints:
    web:
      base-path: /actuator
